# 🚀 התחל כאן - פריסה מהירה

## ⚠️ חשוב: האתר עדיין לא עלה!

אתה צריך לפרוס את האתר כדי לקבל קישור. זה לוקח 5-10 דקות.

## 📋 צעדים מהירים:

### 1. הגדר Git (אם עדיין לא)

פתח PowerShell והרץ:
```powershell
git config --global user.name "השם שלך"
git config --global user.email "your-email@example.com"
```

### 2. Commit את הקבצים

```powershell
git commit -m "Initial commit - My Shopping List App"
```

### 3. צור Repository ב-GitHub

1. לך ל: https://github.com/new
2. שם: `my-shopping-list`
3. **אל תסמן** "Add a README file"
4. לחץ "Create repository"

### 4. העלה את הקוד

```powershell
git remote add origin https://github.com/YOUR_USERNAME/my-shopping-list.git
git branch -M main
git push -u origin main
```

**החלף `YOUR_USERNAME` בשם המשתמש שלך ב-GitHub!**

### 5. פרוס ב-Vercel

1. לך ל: https://vercel.com
2. לחץ "Sign Up" > "Continue with GitHub"
3. לחץ "Add New Project"
4. בחר את `my-shopping-list`
5. לחץ "Deploy"

### 6. קבל את הקישור! 🎉

תוך 2-3 דקות תקבל קישור כמו:
**`https://my-shopping-list.vercel.app`**

---

## 💡 עזרה

אם יש בעיות:
- בדוק את `DEPLOY_NOW.md` להוראות מפורטות
- ודא שהגדרת Git (שלב 1)
- ודא שהעלית את הקוד ל-GitHub (שלבים 3-4)

**אחרי שתפרס, תקבל את הקישור ב-Vercel Dashboard!**
